# ПРАКТИЧЕСКАЯ РАБОТА №4. Указатели и адресная арифметика


## Содержание

🔍 [Теоретический минимум](#Теоретический-минимум)  
💡 [Полезные ссылки](#Полезные-ссылки)  
⚔️ [Учебная задача](#Учебная-задача)  
📋 [Задания](#Задания)  
🚩 [Контрольные вопросы](#Контрольные-вопросы)

*** 

## Теоретический минимум

### Указатели (Pointers)

Указатель — это переменная, значением которой является адрес ячейки памяти.

* `&` (оператор взятия адреса) — позволяет получить адрес переменной.
* `*` (оператор разыменования) — позволяет получить доступ к значению, хранящемуся по адресу.

```cpp
int x = 10;
int* p = &x; // p хранит адрес x
*p = 20;     // теперь x равно 20

```

### Адресная арифметика

Указатели можно изменять. Когда мы прибавляем 1 к указателю (`p++`), он сдвигается не на 1 байт, а на размер типа данных, на который указывает.

* Для `int*` сдвиг будет на 4 байта.
* Для `double*` сдвиг будет на 8 байт.

### Указатели и массивы

Имя массива является константным указателем на его первый элемент.
Выражение `arr[i]` эквивалентно `*(arr + i)`. В данной лабораторной работе **запрещено** использование квадратных скобок `[]` для доступа к элементам.




## Полезные ссылки

1. [Уроки по С++ (ravesli.com)](https://ravesli.com/uroki-cpp/)
2. [Лекции преподавателя МФТИ (youtube.com)](https://www.youtube.com/playlist?list=PLmSYEYYGhnBviRYhIDty-CSTDS16a3whl)
3. [Официальная документация языка ](https://cppreference.com/)


## Учебная задача

### Учебная задача №1: Работа с переменными через указатели

**Задание:** Даны две переменные. Используя только указатели, найти их сумму и заменить меньшее число результатом суммы.

```cpp
#include <iostream>

int main() {
    int a, b;
    int* pa = &a; // Указатель на первую переменную
    int* pb = &b; // Указатель на вторую переменную

    std::cout << "Enter two integers: ";
    std::cin >> *pa >> *pb;

    int sum = *pa + *pb;

    if (*pa < *pb) {
        *pa = sum; // Меняем значение a через указатель
    } else {
        *pb = sum; // Меняем значение b через указатель
    }

    std::cout << "Var A: " << *pa << std::endl;
    std::cout << "Var B: " << *pb << std::endl;

    return 0;
}

```

### Учебная задача №2: Адресная арифметика и массивы

**Задание:** Найти максимальный элемент в массиве, используя только адресную арифметику (без `[]`).

```cpp
#include <iostream>

int main() {
    const int N = 5;
    int arr[N] = {10, 45, 2, 67, 12};
    int* ptr = arr; // Указатель на начало массива
    int* maxPtr = arr; // Указатель на текущий максимум

    for (int i = 1; i < N; i++) {
        // Сравниваем значение по текущему адресу с максимумом
        if (*(ptr + i) > *maxPtr) {
            maxPtr = (ptr + i); // Запоминаем адрес нового максимума
        }
    }

    std::cout << "Max element: " << *maxPtr << std::endl;
    std::cout << "Address of max: " << maxPtr << std::endl;

    return 0;
}

```


## Задания

### Задание 1. Указатели и переменные

**Условие:** Все операции (ввод, расчет, вывод) выполнять строго через разыменование указателей.

| Вар. | Задача | Вар. | Задача |
| --- | --- | --- | --- |
| **1** | Найти произведение двух чисел | **11** | Среднее арифметическое 3-х чисел |
| **2** | Поменять значения двух переменных | **12** | Найти разность квадратов |
| **3** | Вычислить объем куба по ребру `a` | **13** | Проверить, четное ли число |
| **4** | Возвести число в 4-ю степень | **14** | Найти гипотенузу по катетам |
| **5** | Найти большее из двух чисел | **15** | Найти периметр прямоугольника |
| **6** | Вычислить  | **16** | Удвоить значение переменной |
| **7** | Найти остаток от деления A на B | **17** | Вычислить площадь круга () |
| **8** | Найти модуль числа | **18** | Сложить 3 числа и обнулить меньшее |
| **9** | Проверить число на положительность | **19** | Вычислить дискриминант  |
| **10** | Найти среднее геометрическое 2-х чисел | **20** | Возвести число в куб |


### Задание 2. Указатели и массивы (Адресная арифметика)

**Условие:** Дан массив из 8 элементов. Найти результат, используя формулу `*(ptr + i)`. Использование `arr[i]` запрещено.

| Вар. | Задача | Вар. | Задача |
| --- | --- | --- | --- |
| **1** | Сумма всех элементов | **11** | Сумма элементов с четными индексами |
| **2** | Произведение отрицательных | **12** | Количество нулевых элементов |
| **3** | Минимальный элемент | **13** | Найти индекс первого вхождения X |
| **4** | Среднее арифметическое | **14** | Заменить все элементы на их квадраты |
| **5** | Кол-во элементов больше 10 | **15** | Разность первого и последнего |
| **6** | Сумма первого и последнего | **16** | Кол-во элементов, кратных 5 |
| **7** | Найти второй элемент массива | **17** | Проверить, есть ли число K |
| **8** | Произведение элементов < 5 | **18** | Сумма квадратов элементов |
| **9** | Инвертировать массив (реверс) | **19** | Найти Min и увеличить его вдвое |
| **10** | Кол-во положительных элементов | **20** | Среднее между Max и Min |


### Задание 3. Работа с адресами

**Условие:** Вывести значения элементов и их адреса в памяти. Определить расстояние в байтах между соседними элементами.

| Вар. | Тип данных массива | Вар. | Тип данных массива |
| --- | --- | --- | --- |
| **1-7** | `int` | **15-20** | `double` |
| **8-14** | `short` |  |  |


## Контрольные вопросы

1. Что хранится в переменной-указателе?

2. Какой размер занимает указатель в 64-битной системе (независимо от типа данных)?
3. Что произойдет с адресом, если к указателю на `double` прибавить 2?
4. В чем разница между разыменованием `*p` и получением адреса `&p`?
5. Почему обращение по «дикому» (неинициализированному) указателю приводит к краху программы?

