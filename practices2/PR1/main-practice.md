# ПРАКТИЧЕСКАЯ РАБОТА №1. Линейные алгоритмы и битовые сдвиги


## Содержание

🔍 [Теоретический минимум](#Теоретический-минимум)  
💡 [Полезные ссылки](#Полезные-ссылки)  
⚔️ [Учебная задача](#Учебная-задача)  
📋 [Задания](#Задания)  
🚩 [Контрольные вопросы](#Контрольные-вопросы)

*** 

## Теоретический минимум

### Потоковый ввод-вывод и форматирование

В C++ для работы с консолью используется библиотека `<iostream>`. Для управления внешним видом данных (точность, ширина полей) применяется библиотека `<iomanip>`.

* `std::fixed` — принудительный вывод в формате с фиксированной запятой.
* `std::setprecision(n)` — устанавливает `n` знаков после запятой.
* `std::setw(n)` — задает ширину поля вывода (действует только на следующее за ним значение).
* `std::hex`, `std::dec` — переключение между шестнадцатеричной и десятичной системами счисления.

### Побитовые сдвиги

Операции сдвига позволяют манипулировать данными на уровне отдельных бит. Для положительных целых чисел это самый быстрый способ умножения и деления на степени двойки ($2^n$).

* **Сдвиг влево (`<< n`):** эквивалентен умножению на $2^n$. Например, `x << 3` — это $x \cdot 8$.
* **Сдвиг вправо (`>> n`):** эквивалентен целочисленному делению на $2^n$. Например, `x >> 2` — это $x / 4$.


## Полезные ссылки

1. [Уроки по С++ (ravesli.com)](https://ravesli.com/uroki-cpp/)
2. [Лекции преподавателя МФТИ (youtube.com)](https://www.youtube.com/playlist?list=PLmSYEYYGhnBviRYhIDty-CSTDS16a3whl)
3. [Официальная документация языка ](https://cppreference.com/)


## Учебная задача


### Учебная задача №1: Линейный расчет и форматирование

**Условие:** Ввести два вещественных числа $a$ и $b$. Найти их среднее геометрическое  ($\sqrt{a \cdot b}$) и вывести результат, отведя 15 позиций с точностью 4 знака после запятой.

```cpp
#include <iostream>
#include <iomanip>
#include <cmath>

int main() {

    double a, b;
    std::cout << "Enter numbers a and b: ";
    std::cin >> a >> b;

    double res = std::sqrt(a * b);

    std::cout << "Geometric mean:" << std::endl;
    std::cout << std::fixed << std::setprecision(4) << std::setw(15) << res << std::endl;

    return 0;
}

```

### Учебная задача №2: Оптимизация деления сдвигом

**Условие:** Ввести целое число $X$. Разделить его на 16, используя побитовый сдвиг. Вывести результат в десятичном и шестнадцатеричном форматах.

```cpp
#include <iostream>
#include <iomanip>

int main() {

    int x;
    std::cout << "Enter an integer X: ";
    std::cin >> x;

    // 16 = 2^4, следовательно сдвигаем на 4 бита вправо
    int result = x >> 4;

    std::cout << "Result (DEC): " << std::dec << result << std::endl;
    std::cout << "Result (HEX): " << std::hex << std::uppercase << result << std::endl;

    return 0;
}

```

### Учебная задача №3: Комбинированный расчет

**Условие:** Ввести число $A$. Умножить его на 2 через сдвиг и прибавить к нему остаток от деления $A$ на 5.

```cpp
#include <iostream>

int main() {

    int a;
    std::cout << "Enter the number A: ";
    std::cin >> a;

    // Умножение на 2 = сдвиг влево на 1 бит
    int res = (a << 1) + (a % 5);

    std::cout << "Result of combined calculation: " << res << std::endl;

    return 0;
}

```


## Задания

### Задание 1. Форматированный ввод/вывод

**Условие:** Вычислить $Z$. Вывести результат с использованием `setw(15)` и `setprecision(3)`. Переменные $a$ и $b$ вводятся пользователем с клавиатуры.

| Вар. | Формула | Вар. | Формула | Вар. | Формула | Вар. | Формула |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **1** | $Z = \sqrt{a + b}$ | **6** | $Z = \sin(a) \cdot \cos(b)$ | **11** | $Z = \log(a^2)$ | **16** | $Z = a^b$ |
| **2** | $Z = a / (b + 1)$ | **7** | $Z = (a + b)^2$ | **12** | $Z = \sqrt{\\|a-b\\|}$ | **17** | $Z = \tan(a+b)$ |
| **3** | $Z = e^a + b$ | **8** | $Z = 2 \cdot \pi \cdot a$ | **13** | $Z = (a+b)/a$ | **18** | $Z = \cos(a^2)$ |
| **4** | $Z = \\|a\\| + b$ | **9** | $Z = a^3 / b$ | **14** | $Z = 1 / (a \cdot b)$ | **19** | $Z = \sqrt[3]{a}$ |
| **5** | $Z = \ln(b) - a$ | **10** | $Z = \sqrt{a^2 + b^2}$ | **15** | $Z = e^{-a}$ | **20** | $Z = \sin(a+b)$ |


### Задание 2. Оптимизация деления (сдвиг вправо `>>`)

**Условие:** Реализовать деление на константу через сдвиг. Добавить комментарий вида `// n = 2^k`. Переменные $a$ и $b$ вводятся пользователем с клавиатуры.

| Вар. | Операция | Вар. | Операция | Вар. | Операция | Вар. | Операция |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **1** | $a / 2$ | **6** | $(a+10) / 4$ | **11** | $a / 128$ | **16** | $a / 16$ |
| **2** | $a / 4$ | **7** | $(a \cdot 2) / 8$ | **12** | $a / 256$ | **17** | $(a+b) / 2$ |
| **3** | $a / 8$ | **8** | $(a-b) / 2$ | **13** | $(a+5) / 32$ | **18** | $a / 64$ |
| **4** | $a / 16$ | **9** | $a / 32$ | **14** | $(a \cdot a) / 4$ | **19** | $(a-1) / 8$ |
| **5** | $a / 32$ | **10** | $a / 64$ | **15** | $a / 2$ | **20** | $(a+b) / 4$ |


### Задание 3. Умножение через сдвиг (сдвиг влево `<<`)

**Условие:** Вычислить $R = (a \cdot Const) + (a \pmod 5)$. Переменная $a$  вводится пользователем с клавиатуры.

| Вар. | Const | Вар. | Const | Вар. | Const | Вар. | Const |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **1** | 2 | **6** | 64 | **11** | 2 | **16** | 64 |
| **2** | 4 | **7** | 128 | **12** | 4 | **17** | 128 |
| **3** | 8 | **8** | 2 | **13** | 8 | **18** | 2 |
| **4** | 16 | **9** | 4 | **14** | 16 | **19** | 4 |
| **5** | 32 | **10** | 8 | **15** | 32 | **20** | 8 |


## Контрольные вопросы

1. В чем разница между `setw` и `setprecision`?
2. Можно ли заменить деление на 3 побитовым сдвигом? Ответ обоснуйте.
3. Как вывести число в шестнадцатеричном виде заглавными буквами?
4. Для чего нужен `#include`?

