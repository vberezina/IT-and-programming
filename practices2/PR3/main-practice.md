# ПРАКТИЧЕСКАЯ РАБОТА №3. Статические массивы и строки


## Содержание

🔍 [Теоретический минимум](#Теоретический-минимум)  
💡 [Полезные ссылки](#Полезные-ссылки)  
⚔️ [Учебная задача](#Учебная-задача)  
📋 [Задания](#Задания)  
🚩 [Контрольные вопросы](#Контрольные-вопросы)

*** 

## Теоретический минимум

### Статические массивы

**Массив** — это коллекция фиксированного размера, содержащая элементы одного типа, расположенные в памяти последовательно.

* **Индексация:** Всегда начинается с **0**. Последний элемент имеет индекс `N - 1`.
* **Память:** Размер массива должен быть известен на этапе компиляции (константа).
* **Многомерные массивы:** В памяти располагаются «по строкам». Доступ осуществляется через несколько индексов: `matrix[row][col]`.

### Класс `std::string`

В отличие от массивов символов (`char[]`), класс `std::string` обеспечивает безопасную работу с текстом.

* `s.length()` или `s.size()` — возвращает количество символов.
* `getline(cin, s)` — считывает всю строку целиком, включая пробелы.
* Оператор `+` — выполняет конкатенацию (склеивание) строк.



## Полезные ссылки

1. [Уроки по С++ (ravesli.com)](https://ravesli.com/uroki-cpp/)
2. [Лекции преподавателя МФТИ (youtube.com)](https://www.youtube.com/playlist?list=PLmSYEYYGhnBviRYhIDty-CSTDS16a3whl)
3. [Официальная документация языка ](https://cppreference.com/)


## Учебная задача


### Учебная pадача №1: Одномерный массив

**Задание:** Найти среднее арифметическое элементов массива и количество элементов меньше среднего.

```cpp
#include <iostream>

int main() {
    const int SIZE = 5;
    int arr[SIZE];
    double sum = 0;

    // Ввод данных
    std::cout << "Enter " << SIZE << " integers: ";
    for (int i = 0; i < SIZE; i++) {
        std::cin >> arr[i];
        sum += arr[i]; // Накапливаем сумму
    }

    double average = sum / SIZE;
    int count = 0;

    // Поиск элементов меньше среднего
    for (int i = 0; i < SIZE; i++) {
        if (arr[i] < average) {
            count++;
        }
    }

    // Вывод результатов
    std::cout << "Average: " << average << std::endl;
    std::cout << "Elements less than average: " << count << std::endl;

    return 0;
}

```

### Учебная pадача №2: Двумерный массив (Матрица)

**Задание:** Вычислить сумму элементов главной диагонали квадратной матрицы 3x3.

```cpp
#include <iostream>

int main() {
    int matrix[3][3];
    int diagonalSum = 0;

    // Заполнение матрицы пользователем
    std::cout << "Enter matrix 3x3 elements:" << std::endl;
    for (int i = 0; i < 3; i++) {
        for (int j = 0; j < 3; j++) {
            std::cout << "mat[" << i << "][" << j << "]: ";
            std::cin >> matrix[i][j];
        }
    }

    // Расчет суммы главной диагонали (где индекс строки == индексу столбца)
    for (int i = 0; i < 3; i++) {
        diagonalSum += matrix[i][i];
    }

    std::cout << "Main diagonal sum: " << diagonalSum << std::endl;

    return 0;
}

```

### Учебная pадача №3: Строки

**Задание:** Заменить все пробелы в строке на символ подчеркивания.

```cpp
#include <iostream>
#include <string>

int main() {
    std::string text;

    std::cout << "Enter a sentence: ";
    std::getline(std::cin, text); // Читаем строку с пробелами

    // Перебор строки как массива символов
    for (int i = 0; i < text.length(); i++) {
        if (text[i] == ' ') {
            text[i] = '_'; // Замена символа
        }
    }

    std::cout << "Result: " << text << std::endl;

    return 0;
}

```


## Задания

### Задание 1. Одномерные массивы

**Условие:** Реализовать алгоритм обработки статического массива из 10 элементов.



| Вар. | Задача | Вар. | Задача |
| --- | --- | --- | --- |
| **1** | Сумма положительных элементов | **11** | Кол-во элементов, кратных 3 |
| **2** | Произведение ненулевых элементов | **12** | Минимальный элемент и его индекс |
| **3** | Максимальный элемент массива | **13** | Разность между Max и Min |
| **4** | Кол-во четных элементов | **14** | Среднее арифметическое отрицательных элементов |
| **5** | Инверсия (реверс) массива | **15** | Замена отрицательных на 0 |
| **6** | Поиск заданного числа X | **16** | Проверка: отсортирован ли массив |
| **7** | Сумма элементов с нечетными индексами | **17** | Кол-во элементов больше числа K |
| **8** | Второй по величине элемент | **18** | Сумма квадратов всех элементов |
| **9** | Перестановка Max и первого элемента | **19** | Поиск элемента, который встречается чаще других (мода)  |
| **10** | Кол-во уникальных элементов | **20** | Удаление/обнуление дубликатов |



### Задание 2. Многомерные массивы

**Условие:** Обработать квадратную матрицу 4x4.


| Вар. | Задача | Вар. | Задача |
| --- | --- | --- | --- |
| **1** | Сумма элементов каждой строки | **11** | Минимальный элемент в 3-й строке |
| **2** | Среднее значение каждого столбца | **12** | Поиск Max элемента на побочной диагонали |
| **3** | Транспонирование матрицы | **13** | Замена отрицательных элементов на 1 |
| **4** | Кол-во нулей во всей матрице | **14** | Сумма элементов выше главной диагонали |
| **5** | Проверка матрицы на симметричность | **15** | Поиск индекса строки с Max суммой |
| **6** | Перестановка 1-й и последней строк | **16** | Произведение элементов 2-го столбца |
| **7** | Сумма элементов по периметру | **17** | Проверка наличия отрицательных чисел |
| **8** | Максимальный элемент в каждом столбце | **18** | Кол-во четных чисел в матрице |
| **9** | Разность сумм двух диагоналей | **19** | Обнуление элементов ниже побочной диагонали |
| **10** | Сумма элементов побочной диагонали | **20** | Среднее арифметическое всей матрицы |



### Задание 3. Работа со строками

**Условие:** Реализовать манипуляции с объектом `std::string`.

| Вар. | Задача | Вар. | Задача |
| --- | --- | --- | --- |
| **1** | Подсчет количества слов | **11** | Удаление всех цифр из строки |
| **2** | Проверка на палиндром | **12** | Найти самое длинное слово |
| **3** | Перевод всей строки в UPPERCASE | **13** | Кол-во заглавных латинских букв |
| **4** | Подсчет знаков препинания | **14** | Замена "cat" на "dog" в тексте |
| **5** | Вывод строки в обратном порядке | **15** | Кол-во гласных букв |
| **6** | Удаление лишних пробелов | **16** | Проверка на содержание только цифр |
| **7** | Поиск самого короткого слова | **17** | Дублирование каждого символа |
| **8** | Подсчет вхождений символа 'a' | **18** | Склеивание двух введенных строк |
| **9** | Извлечение цифр в новую строку | **19** | Удаление первого и последнего слов |
| **10** | Переворот каждого слова в строке | **20** | Шифрование сдвигом ASCII на +1 |




## Контрольные вопросы

1. В чем заключается риск выхода за границы статического массива?

3. В чем разница между `cin >> str` и `getline(cin, str)`?
4. Как вычислить количество строк и столбцов в двумерном статическом массиве?
5. Каким образом `std::string` хранит данные в памяти (динамически или статически)?

